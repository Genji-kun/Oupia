export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
    OTHER = "OTHER"
}

export enum UserRole {
    LANDLORD = "ROLE_LANDLORD",
    TENANT = "ROLE_TENANT",
    ADMIN = "ROLE_ADMIN"
}

export enum VoteType {
    LANDLORD = "LANDLORD",
    TENANT = "TENANT"
}

export enum Vote {
    ACCEPT = "ACCEPT",
    DENIED = "DENIED",
    UNKNOWN = "UNKNOWN"
}