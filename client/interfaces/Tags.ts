export interface Amenity {
    amenityName: string,
}

export interface TagPrice {
    minPrice: number,
    maxPrice: number
}


export interface TagLocation {
    fullLocation: string,
    latitute: number,
    longtitude: number
}